-- Ручной порядок остановок внутри рейса. Пусто = порядок не задавали руками,
-- считается по маршруту направления, иначе по времени плеча.
-- Аддитивно: nullable-колонка, существующие строки не меняются.
ALTER TABLE "TripCargoUnit" ADD COLUMN "stopOrder" INTEGER;
