-- Перечисление вертикалей груза рейса (MONO/MIX выводится из длины списка).
-- Аддитивно: только добавление колонки с пустым массивом по умолчанию.
-- Существующие рейсы намеренно НЕ пересчитываются (решение заказчика) — у них
-- список остаётся пустым, а источником правды остаётся tripType.
ALTER TABLE "Trip" ADD COLUMN "verticalCodes" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
