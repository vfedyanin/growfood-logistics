# Журнал сессий — bald-logist

Только дописывается, свежие сверху.

---

## 10.08.2026 — открыт доступ к GitHub, спроектировано автораспределение

**Инфраструктура.** Полдня ушло на разбор, почему не проходит push. Оказалось —
приложение Claude было авторизовано, но не установлено ни на одном аккаунте.
Вася установил его на `vfedyanin` с правом записи, push прошёл. Разбор и ложные
версии — в `DECISIONS.md`, чтобы второй раз этот путь не проходить.

**Что сделано.**
- Папка памяти доставлена в репозиторий (коммит `aef52c4`).
- Спроектировано автораспределение плеч: шесть решений приняты, три вопроса
  остались открытыми — всё в `CONTEXT.md`.
- Разобрано, что уже есть в коде: шаблоны рейсов, многоостановочность, ручная
  привязка плеч.

**Ошибка, которую допустил.** На просьбу об отчёте по незаполненным объёмам
построил страницу в интерфейсе вместо того, чтобы отдать цифры в чат. Причина
была в отсутствии доступа к базе, но решение всё равно не то, о чём просили.
Коммит `65187a7` откачен из ветки.

**Чем закончили.** Ждём три ответа по шаблонам рейсов (жёсткий маршрут или
каркас, один шаблон или несколько, откуда брать типы ТС) и строку подключения к
базе от Васи. Код не начинать до ответов.

---

## 06.08.2026 — восстановление контекста после потери ноутбука

**Ситуация.** Ноутбук, на котором велась работа, вышел из строя. Сессия начата с
нуля из клона репозитория; вся незакоммиченная память потеряна.

**Что сделано.**
- Проведена ревизия репозитория: git-история, ветки, схема Prisma, модуль
  планирования, логика привязки плеч к рейсам.
- Установлено, что автоматического распределения плеч в коде нет — ни функции,
  ни ветки, ни незакоммиченных изменений. Работа над ним не начиналась либо
  велась только на словах.
- Заведена эта папка памяти, `CONTEXT.md` подключён к корневому `CLAUDE.md`
  через `@`-импорт.

**Потери.** `SESSION_CONTEXT.txt` не восстановим — он в `.gitignore` (помечен
как содержащий секреты) и существовал только локально. На него ссылаются как на
источник данных: пункт #28 бэклога (тарифы Юми, договор Д-008/2026), пункт #34,
блок сессии 03.08. Эти данные придётся вводить заново.

**Чем закончили.** Семь открытых вопросов по автораспределению плеч — см.
`CONTEXT.md`. Без ответов на них проектирование не начинать.
